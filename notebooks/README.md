# Notebooks

These notebooks are optional exploration/documentation companions. The production pipeline does not depend on them. They use the real local IEEE-CIS files through `src.data.loader`; no sample dataset is bundled.

For the reproducible end-to-end run, use:

```powershell
python -m src.models.train
python -m src.models.predict
```
